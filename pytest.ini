[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --import-mode=importlib
pythonpath = .
markers =
    integration: marks tests that require network (e.g. Wikipedia); skip with -m "not integration"
