# Core dependencies for Keyword_extraction tool
# Only includes packages actually used in the code

# HTML parsing and text extraction
beautifulsoup4==4.12.3

# AI/NLP models and transformers
transformers==4.44.2

# Data manipulation and CSV export
pandas==2.0.3

# Progress bars for batch processing
tqdm==4.66.5

# PyTorch backend for transformers
torch==2.4.1

# Note: The following packages are automatically installed as dependencies:
# - numpy (required by pandas and torch)
# - tokenizers (required by transformers)
# - huggingface-hub (required by transformers)
# - requests (required by huggingface-hub)
# - Other standard library packages (os, re, collections, argparse)
